# Copy to .env.local and set values for local overrides.
# For production build, set these in your CI/hosting environment (e.g. Vite injects at build time).

# Backend API base URL (must end with /api). Required in production.
# Example: https://api.yourerp.com/api
VITE_API_URL=http://localhost:8080/api
